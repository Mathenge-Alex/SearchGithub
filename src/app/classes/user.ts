export class User {
  constructor(
    public avatar_url:string, public name:string, public html_url:string, public bio:string, public login:string, public following:number, public followers:number, 
  ){}
}
