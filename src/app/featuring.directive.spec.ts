import { FeaturingDirective } from './featuring.directive';

describe('FeaturingDirective', () => {
  it('should create an instance', () => {
    const directive = new FeaturingDirective();
    expect(directive).toBeTruthy();
  });
});
