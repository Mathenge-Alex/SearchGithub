import { FormNavigtionDirective } from './form-navigtion.directive';

describe('FormNavigtionDirective', () => {
  it('should create an instance', () => {
    const directive = new FormNavigtionDirective();
    expect(directive).toBeTruthy();
  });
});
